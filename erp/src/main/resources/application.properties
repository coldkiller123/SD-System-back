# Application name
spring.application.name=erp

# Database connection pool type
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource

# Database driver class
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# DevTools configuration - auto-restart when these paths change
spring.devtools.restart.additional-paths=src/main/java
spring.devtools.restart.enabled=true

# MyBatis-Plus configuration - log SQL statements to console
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# Server port configuration
server.port=8080

# Database connection URL
spring.datasource.url=jdbc:mysql://121.199.165.179:3306/erp
# Local database connection URL (commented out)
#spring.datasource.url=jdbc:mysql://localhost:3306/erp?useSSL=false

# Database credentials
spring.datasource.username=root
spring.datasource.password=Erp666666

# MyBatis-Plus configuration - disable underscore to camel case mapping
mybatis-plus.configuration.map-underscore-to-camel-case=false

# Disable JMX to avoid potential conflicts
spring.jmx.enabled=false

# File upload configuration
# Local file upload path (commented out)
#file.upload.base-path=C:/Users/HUAWEI/Desktop/threexiao/back/SD-System-back/erp/static/uploads
# Linux server file upload path
file.upload.base-path=/usr/app/static/uploads

# Maximum file size for uploads
spring.servlet.multipart.max-file-size=10MB

# Maximum request size for multipart requests
spring.servlet.multipart.max-request-size=50MB

# Email configuration (QQ mail)
spring.mail.host=smtp.qq.com
spring.mail.username=3507247177@qq.com
spring.mail.password=rugadawlkspwcihf
spring.mail.port=587
spring.mail.default-encoding=UTF-8

# Email timeout configurations
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connection-timeout=5000
spring.mail.properties.mail.smtp.write-timeout=5000

# Email security configurations
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.auth=true

# Enable email debugging
spring.mail.properties.mail.debug=true